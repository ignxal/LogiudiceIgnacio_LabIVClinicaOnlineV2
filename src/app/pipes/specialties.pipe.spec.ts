import { SpecialtiesPipe } from './specialties.pipe';

describe('SpecialtiesPipe', () => {
  it('create an instance', () => {
    const pipe = new SpecialtiesPipe();
    expect(pipe).toBeTruthy();
  });
});
