import { SurveyValuesPipe } from './survey-values.pipe';

describe('SurveyValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new SurveyValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
